- name: List build output
        run: find "$GITHUB_WORKSPACE/qmk_firmware/.build" -type f | sort

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: sofle-helios-firmware
          path: ${{ github.workspace }}/qmk_firmware/.build/
